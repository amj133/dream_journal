<%= form_for([current_user, @dream]) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>

  <%= f.label :body %>
  <%= f.text_area :body %><br>

  <%= f.label :analysis %>
  <%= f.text_area :analysis %><br>

  <%= f.label "Category" %>
  <%= collection_select :dream, :category_ids, Category.all, :id, :name, {}, {multiple: true} %><br>

  <%= f.submit %>
<% end %>
